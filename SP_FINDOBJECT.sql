CREATE PROCEDURE [dbo].[SP_FINDOBJECT]

	@pOBJECT VARCHAR(MAX)

AS

BEGIN

	SELECT	OBJECT_ID,

			NAME,

			TYPE_DESC 

	FROM	SYS.OBJECTS 

	WHERE	NAME LIKE '%' + @pOBJECT + '%'

	ORDER BY

			TYPE_DESC,

			NAME

END
